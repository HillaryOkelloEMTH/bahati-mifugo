server:
  error:
    include-message: always
  port: 9600

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/dairy
    username: ibrahim
    password: kali
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    properties:
      hibernate:
        #        dialect: org.hibernate.dialect.MariaDB53Dialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  banner:
    location: classpath:static/banner.txt